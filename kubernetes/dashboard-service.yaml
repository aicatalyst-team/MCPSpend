apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: autopoc-mcpspend
  labels:
    app: dashboard
    autopoc.io/project: mcpspend
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
    - port: 3000
      targetPort: 3000