apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: autopoc-mcpspend
  labels:
    app: postgres
    autopoc.io/project: mcpspend
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432