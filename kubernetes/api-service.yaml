apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: autopoc-mcpspend
  labels:
    app: api
    autopoc.io/project: mcpspend
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 4000
      targetPort: 4000